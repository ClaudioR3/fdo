'''
Created on May 31, 2016

@author: claudio
'''
documento=open("config_datas.txt","r")
lines=documento.readlines()
for l in lines:
    parola=l.split(" ")
    prec=""
    for p in parola:
        if prec=="host":
            self.setHost(k)
        if prec=="user":
                        self.setUser(k)
                    if prec=="passwd":
                        self.setPasswd(k)
                    if prec=="db":
                        self.setDB(k)